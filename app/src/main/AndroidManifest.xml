<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.task71">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Task71">
        <activity android:name=".MainActivity$UpdateActivity"
            android:parentActivityName=".MainActivity$Notes"></activity>
        <activity
            android:name=".MainActivity$AddActivity"
            android:label="Add Notes"
            android:parentActivityName=".MainActivity$Notes" />
        <activity
            android:name=".MainActivity$Notes"
            android:label="Notes List"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:label="Interface">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>